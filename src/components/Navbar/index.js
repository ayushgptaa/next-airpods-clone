import styles from "./index.module.scss"

const Navbar = () => {
    return (
        <header className={styles.header}>
            <nav>
                <h3>AirPods Max</h3>
            </nav>
        </header>
    )
}

export default Navbar
